package pivothead.som.pivotserver;

import android.content.Intent;
import android.os.Bundle;
import android.util.Log;

import org.renpy.android.PythonService;


/**
 * Created by user on 2018/3/5.
 */

public class ServiceWebservice extends PythonService {
    private final static String TAG = ServiceWebservice.class.getSimpleName();
    @Override
    public int onStartCommand(Intent intent, int flags, int startId) {
        return super.onStartCommand(intent, flags, startId);
    }
}
